# function for checking and loading required packages
loadPkg <- function(pkgname){
# require() is the same as library() but returns a logical.
# character.only= TRUE means pkgname is the name of the package.
isInstalled <- require(pkgname,character.only = TRUE)
# If the package has not been installed yet, then install and try again.
if (!isInstalled) {install.packages(pkgname); library(pkgname,character.only=TRUE)}
}
# We will need the following libraries
loadPkg("DBI")
loadPkg("RSQLite")
# Open an SQLite connection using the filename shown
conn <- dbConnect(RSQLite::SQLite(),"CargoIS_Data/sandboxdata.db")
# Close the connection.
dbDisconnect(conn)
# function for checking and loading required packages
loadPkg <- function(pkgname){
# require() is the same as library() but returns a logical.
# character.only= TRUE means pkgname is the name of the package.
isInstalled <- require(pkgname,character.only = TRUE)
# If the package has not been installed yet, then install and try again.
if (!isInstalled) {install.packages(pkgname); library(pkgname,character.only=TRUE)}
}
# We will need the following libraries
loadPkg("DBI")
loadPkg("RSQLite")
# Open an SQLite connection using the filename shown
conn <- dbConnect(RSQLite::SQLite(),"CargoIS_Data/CargoIS.db")
# Close the connection.
dbDisconnect(conn)
# function for checking and loading required packages
loadPkg <- function(pkgname){
# require() is the same as library() but returns a logical.
# character.only= TRUE means pkgname is the name of the package.
isInstalled <- require(pkgname,character.only = TRUE)
# If the package has not been installed yet, then install and try again.
if (!isInstalled) {install.packages(pkgname); library(pkgname,character.only=TRUE)}
}
# We will need the following libraries
loadPkg("DBI")
loadPkg("RSQLite")
# Open an SQLite connection using the filename shown
conn <- dbConnect(RSQLite::SQLite(),"CargoIS_Data/CargoIS_data.db")
# Close the connection.
dbDisconnect(conn)
load("C:/ASI_UROP/IATA_Database/.RData")
# function for checking and loading required packages
loadPkg <- function(pkgname){
# require() is the same as library() but returns a logical.
# character.only= TRUE means pkgname is the name of the package.
isInstalled <- require(pkgname,character.only = TRUE)
# If the package has not been installed yet, then install and try again.
if (!isInstalled) {install.packages(pkgname); library(pkgname,character.only=TRUE)}
}
# We will need the following libraries
loadPkg("DBI")
loadPkg("RSQLite")
# Open an SQLite connection using the filename shown
conn <- dbConnect(RSQLite::SQLite(),"CargoIS_Data/CargoIS_data.db")
# Close the connection.
dbDisconnect(conn)
sgexports <- read.csv("CargoIS_Data/export_to_singapore")
sgexports <- read.csv("/CargoIS_Data/export_to_singapore")
sgexports <- read.csv("/CargoIS_Data/export_to_singapore.csv")
sgexports <- read.csv("CargoIS_Data/export_to_singapore.csv")
pairs(sgexports)
View(sgexports)
sapply(sgexports, class)
sgexports <- read.csv("CargoIS_Data/export_to_singapore.csv")
pairs(sgexports)
View(sgexports)
sgexports <- read.csv("CargoIS_Data/export_to_singapore.csv")
pairs(sgexports)
pairs(sgexports, pch=21)
# function for checking and loading required packages
loadPkg <- function(pkgname){
# require() is the same as library() but returns a logical.
# character.only= TRUE means pkgname is the name of the package.
isInstalled <- require(pkgname,character.only = TRUE)
# If the package has not been installed yet, then install and try again.
if (!isInstalled) {install.packages(pkgname); library(pkgname,character.only=TRUE)}
}
# We will need the following libraries
loadPkg("DBI")
loadPkg("RSQLite")
# Open an SQLite connection using the filename shown
conn <- dbConnect(RSQLite::SQLite(),"CargoIS_Data/CargoIS_data.db")
# Close the connection.
dbDisconnect(conn)
ASEAN2015 <- read_csv("data/CargoIS_Data/ASEAN_2015_aggregate_imports.csv")
loadPkg("tidyverse")
install.packages(pkgname)
# function for checking and loading required packages
loadPkg <- function(pkgname){
# require() is the same as library() but returns a logical.
# character.only= TRUE means pkgname is the name of the package.
isInstalled <- require(pkgname,character.only = TRUE)
# If the package has not been installed yet, then install and try again.
if (!isInstalled) {install.packages(pkgname); library(pkgname,character.only=TRUE)}
}
# loading required packages
loadPkg("DBI")
loadPkg("RSQLite")
loadPkg("tidyverse")
